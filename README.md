*原理：*
二维的热力图升维到向量空间以后使语义访问可审计。

为了安全，我并没有允许热力对搜索进行加权，因为这会重新带来自指，我面对指数级的复杂度退缩了。热力目前用于在内存与硬盘之间翻页。

我自己的项目里，目前对于热力的想法只有动态注入prompt和可审计性。

此外相似度>0.95则完全不储存，因为这个记忆模块没有时间的概念，最开始的版本有，但是我意识到AI的记忆模块里的时间与热力实际上是矛盾的。

算了，就这么多！
